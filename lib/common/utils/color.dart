import 'package:flutter/material.dart';

class AppColors {
  /// 主题色
  static const Color themeColor = Color.fromRGBO(228, 2, 127, 1.0);

  /// 背景色
  static const Color primaryBgColor = Color.fromRGBO(237, 237, 237, 1.0);

  /// 标题颜色
  static const Color primaryTitleColor31 = Color.fromRGBO(31, 31, 31, 1.0);

  /// 副标题颜色
  static const Color primarySubTitleColor153 =
      Color.fromRGBO(153, 153, 153, 1.0);

  /// 黄色
  static const Color primaryYellowColor = Color.fromRGBO(248, 171, 4, 1.0);

  /// 白色
  static const Color white = Colors.white;

  static const Color primarySubTitleColor242 =
      Color.fromRGBO(242, 242, 242, 1.0);
  static const Color primarySubTitleColor212 =
      Color.fromRGBO(212, 212, 212, 1.0);
}
